env:
  node: true

extends:
  - 'airbnb-base'
  - 'prettier'

plugins:
  - 'prettier'

parserOptions:
  ecmaVersion: latest

rules:
  no-console: 0
  import/extensions:
    - error
    - ignorePackages
    - js: always
  prettier/prettier: "error"