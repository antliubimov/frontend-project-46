name: linter and jest test
on: [push]
jobs:
  lint-jest-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use node.js
      - uses: actions/setup-node@v3
        with:
          node-version: 16
      - name: npm install
      - run: make lint

